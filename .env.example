# WordPress MCP Server Configuration
# Copy this file to mcp-server/.env and fill in your actual values

# === WORDPRESS CONNECTION ===
# Your WordPress site URL (without trailing slash)
WP_SITE_URL=https://your-wordpress-site.com

# WordPress username with admin privileges
WP_USERNAME=your_admin_username

# WordPress Application Password
# Generate from: WordPress Admin → Users → Your Profile → Application Passwords
# Format: xxxx xxxx xxxx xxxx xxxx xxxx (spaces are optional)
WP_APP_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx

# === API CONFIGURATION ===
# API timeout in seconds (default: 30)
API_TIMEOUT=30

# Rate limit - requests per minute (default: 60)
RATE_LIMIT=60

# === CORS CONFIGURATION === 
# Comma-separated list of allowed origins (optional)
# Example: https://app1.com,https://app2.com
CORS_ALLOWED_ORIGINS=

# === BACKUP SETTINGS ===
# Days to keep template backups (default: 7)
BACKUP_RETENTION_DAYS=7

# === DEBUG SETTINGS ===
# Enable debug logging (true/false)
MCP_DEBUG=false

# === MONITORING ===
# Health check port (optional, for monitoring.py)
HEALTH_CHECK_PORT=8080

# === QUICK SETUP INSTRUCTIONS ===
# 1. Copy this file to: mcp-server/.env
# 2. Fill in your WordPress credentials
# 3. Generate Application Password in WordPress:
#    - Go to Users → Your Profile
#    - Scroll to "Application Passwords"
#    - Enter name: "MCP Server"
#    - Click "Add New Application Password"
#    - Copy the generated password to WP_APP_PASSWORD above
# 4. Save the file
# 5. Test connection: cd mcp-server && python test_connection.py
